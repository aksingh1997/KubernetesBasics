apiVersion: v1
kind: Service
metadata:
  name: {{ include "KubernetesBasicsChart.fullname" . }}
  labels:
    {{- include "KubernetesBasicsChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "KubernetesBasicsChart.selectorLabels" . | nindent 4 }}
