apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "KubernetesBasicsChart.fullname" . }}-test-connection"
  labels:
    {{- include "KubernetesBasicsChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "KubernetesBasicsChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
